---
interface Props {
  class?: string;
  'data-testid'?: string;
}

const { class: className = '', 'data-testid': dataTestId } = Astro.props;
---

<ul class={`list-inside list-none space-y-2 ${className}`} data-testid={dataTestId}>
  <slot />
</ul>
