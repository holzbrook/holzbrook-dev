---
interface Props {
  class?: string;
}

const { class: className }: Props = Astro.props;
---

<section class={`py-12 w-full max-w-screen-md mx-auto ${className}`}>
  <div class="w-full max-w-6xl mx-auto px-4 sm:px-6 lg:px-8">
    <slot />
  </div>
</section>
