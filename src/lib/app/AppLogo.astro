---
interface Props {
  class?: string;
}

const { class: className }: Props = Astro.props;
---

<div class={`${className} flex items-center`}>
  <strong class="text-base md:text-lg lg:text-xl font-extrabold text-white">Hol(z)brook</strong>
</div>
